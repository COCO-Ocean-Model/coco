      INTEGER   NWORK
      PARAMETER(NWORK = 25*NXYZDM)

      REAL*8  WRKOUT(NWORK)
      REAL*8  DBLEOU(NXYZ)
      REAL*4  SNGLOU(NXYZ)
      SAVE WRKOUT, DBLEOU, SNGLOU

#ifdef OPT_PARALLEL
      REAL*8, ALLOCATABLE ::  DBLOUG(:, :, :)
      REAL*4, ALLOCATABLE ::  SNGOUG(:, :, :)
      SAVE DBLOUG, SNGOUG
#endif
